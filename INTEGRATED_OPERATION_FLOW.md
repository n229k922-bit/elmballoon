# elm・balloon 統合運用フロー

## 目標

お客様には自然で早い接客を行い、店長には判断が必要な内容だけを分かりやすく届ける。HP、顧客LINE、統括マネージャーLINE、商品登録、営業日、Googleカレンダーは同じ注文データを参照し、二重登録や確認漏れを防ぐ。

## 全体構成

```text
お客様LINE ─┐
HP注文導線 ─┼→ 統合Worker → D1（顧客・注文・営業日・監査）
店長LINE  ──┘       ├→ KV（会話状態・確認トークン）
                     ├→ R2（商品・参考画像）
                     └→ Googleカレンダー（承認後の予定）
                              ↓
                         HP公開API
```

## 1. お客様の注文フロー

1. お客様がHPの「ご注文・ご相談はこちら」または公式LINEから相談する。
2. 注文担当が自然な会話で、用途・商品タイプ・希望日・希望時間・予算・受取方法を聞く。
3. 画像が送られた場合は、注文スレッドへ参考画像として紐付ける。
4. 必須項目が揃ったら、お客様へ内容を復唱する。
5. 希望日を営業日テーブルと照合し、休業日・時間外なら先に代替候補を案内する。
6. Googleカレンダーの空き時間を確認し、店長確認待ちにする。
7. 店長が可否・価格・納期・配達料を判断する。
8. 統括マネージャーが店長判断を自然な文章でお客様へ伝える。
9. お客様の注文意思を確認後、注文カードを`production_confirmed`へ進める。
10. 承認された受取・制作・配達予定だけをGoogleカレンダーへ登録する。

## 2. 店長LINEの理想フロー

店長は複雑なコマンドを覚えず、統括マネージャーから届く確認フォームに返信する。

```text
【店長確認】
お客様：田中さん
希望日：2026年10月10日 14:00
商品：誕生日バルーン
予算：5,500円
確認事項：制作可否・価格・受取可否

返信例：制作可。5,500円。14時受取可能。
```

判断が必要なものだけを通知し、単なる記録や重複通知は送らない。

## 3. LINE画像からの商品登録フロー

1. 許可済みの統括マネージャーLINEから商品画像を送る。
2. 「商品登録を開始します」と返し、画像をR2へ保存する。
3. 商品名、カテゴリー、価格、説明、掲載可否を順番に確認する。
4. 入力内容と画像のサムネイル情報を復唱する。
5. 店長が「登録」と返信したら、D1の商品を`published`にする。
6. HPの商品APIが次回表示時に公開商品を読み込む。
7. 「非掲載」「削除」は物理削除せず`archived`にする。

画像だけで価格や在庫を推測して公開しない。価格・納期・在庫・掲載可否は必ず店長確認を必要とする。

## 4. 店休日・営業時間変更フロー

1. 店長が「明日は休み」「10月10日は10時〜14時営業」と送る。
2. 統括マネージャーが絶対日付と変更内容を復唱する。
3. 店長が「確定」と返信する。
4. D1の`business_schedule`を更新し、監査ログへ保存する。
5. HPの営業日APIへ反映する。
6. 今後の注文受付時に新しい営業日情報を参照する。

営業日変更は注文・カレンダー・HP表示のすべてで同じD1データを使う。静的JSONはAPI障害時だけのフォールバックとする。

## 5. 状態管理

### 注文

`new → collecting → owner_review → proposing → awaiting_customer_confirmation → production_confirmed → production_in_progress → ready_for_fulfillment → fulfilled`

### 商品

`draft → needs_confirmation → published → archived`

### 営業日変更

`proposed → owner_confirmed → applied → audited`

状態を飛ばして公開・確定しない。各状態変更は一度だけ処理し、LINEの同じイベントが再送されても二重登録しない。

## 6. 競合とエラーへの対応

- 同じLINEイベントは`webhook_event_id`で重複排除する。
- 同じ注文カードは注文スレッドごとに1件だけ保持する。
- 休業日と注文希望日が衝突したら自動確定せず、店長確認へ戻す。
- Googleカレンダーの予定が取得できない場合は「確認不能」とし、空きと判断しない。
- R2保存に失敗した場合は商品を公開しない。
- Worker API停止時はHPに「最新情報を取得できない」旨を表示する。
- すべての書込みに変更前・変更後・実行者・時刻・結果を残す。
- LINEシークレット、アクセストークン、画像の非公開キーはGitHubへ保存しない。

## 7. HPの理想画面

1. ファーストビューに「何の店か」と注文導線を表示する。
2. 営業日カレンダーには最新取得日時を表示する。
3. 商品一覧は公開済み商品だけをAPIから表示する。
4. 商品をカテゴリー・価格帯で絞り込めるようにする。
5. 商品ページからLINE相談へ商品情報を引き継ぐ。
6. API障害時は古い情報を黙って表示せず、注意表示を出す。

## 推奨実装順

1. Worker本番デプロイと営業日APIの疎通確認
2. 注文希望日と営業日・Googleカレンダーの統合判定
3. 店長確認後のGoogleカレンダー登録
4. LINE画像の商品登録（R2・D1・確認フロー）
5. 商品公開APIとHP一覧の接続
6. LINE・HP・カレンダーを使った総合テスト
