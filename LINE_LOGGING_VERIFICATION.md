# LINE公式アカウント記録の確認手順

## 目的

AIがLINEのやり取りを取得・記録・返信できているかを、実装前後で同じ手順で確認する。

## 記録対象

1. お客様の受信メッセージ
2. お客様から届いた画像の受信記録
3. AIが作成した返信案
4. 店長が承認して送信した返信
5. 店長確認・変更承認・保留の操作
6. 注文書・注文カードへの反映結果

## 確認フロー

```text
LINEでテストメッセージ送信
        ↓
Webhookが200 OKを返す
        ↓
order_messagesに受信行が追加される
        ↓
customer_reply_reviewsに返信案が追加される
        ↓
店長が送信操作
        ↓
order_messagesにassistant_outboundが追加される
        ↓
注文カード・注文書の状態が更新される
```

## 合格条件

- 受信メッセージに顧客ID・注文スレッドID・受信時刻がある
- 画像の場合も受信イベントが記録される
- AI返信案が重複登録されない
- 店長が送信した本文と送信時刻が記録される
- 店長の直接対応で変更が発生した場合、旧内容と新内容が残る
- 店長承認前に注文書が上書きされない
- LINEから直接送ったメッセージがWebhookに届かない場合、その制限を記録する

## 実装前の確認方法

- 現在のLINE公式アカウントでテストメッセージを1件送る
- Webhookログでイベント受信を確認する
- D1の `order_messages`、`customer_reply_reviews`、`customer_order_threads` を確認する
- 店長の送信操作後に送信本文が保存されているか確認する
- 同じイベントを再送しても二重記録されないことを確認する

## 注意事項

- Messaging APIだけでは、Webhook導入前の過去トーク履歴を取得できない
- LINE管理画面から店長が直接送ったメッセージは、Webhookに届かない場合がある
- 画像ファイル本体の保存と、画像を受信した記録は別管理にする
- 電話番号・住所などの個人情報は、必要な注文だけに紐づけて保存する
