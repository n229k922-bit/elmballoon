# 注文受付AI・予定候補連携仕様

## 目的

お客様向け公式LINEの新しい受信メッセージを注文担当が整理し、注文内容・希望日・受取／配達／来店を注文記録へまとめる。予定は自動登録せず、統括マネージャーが店長用LINEで承認を得てからスケジュール担当へ渡す。

## 守る境界

- お客様向け公式LINEと店主用のバルーン秘書LINEは別チャネルとして扱う。
- 注文担当は注文候補を作るが、価格・在庫・納期・注文確定を断定しない。
- スケジュール担当だけが、統括マネージャーと店長の承認後に業務カレンダー登録を行う。
- ホームページを更新するのはホームページ管理AIだけである。
- 家族予定の閲覧・変更は行わない。

## 実行フロー

```text
お客様の受信メッセージ
  → 注文担当が注文スレッドへ記録・要約
  → 受取／配達／来店の候補日を検出
  → スケジュール担当が重複・近い空き時間を確認
  → 統括マネージャーが店長LINEへ候補を報告
  → 店長が候補を選び、明示承認
  → スケジュール担当が業務カレンダーへ登録
  → 統括マネージャーが店長へ完了報告
```

## 確定の扱い

お客様の「大丈夫です」「その日でお願いします」だけでは、予定を自動登録しない。店長の案内内容がWebhookだけでは取得できない可能性があるため、店長LINEで次を復唱する。

> 田中様／受取／2026-09-24 14:00 の候補を検出しました。業務カレンダーへ登録しますか？

店長の明示承認後だけ、`elm・balloon｜確定予定` または `elm・balloon｜制作・準備` への登録を許可する。

## 手動返信との両立

店長がLINE Official Account Managerから手動返信した文章は、現在のWebhook受信だけでは注文受付AIへ自動記録されない。そのため段階導入では、受信したお客様の内容を注文受付AIが要約し、店長が店主LINEで承認・補足する。

会話全体を完全に記録する必要が出た段階では、注文受付AIが作成する返信案を店長が承認して送信する注文画面を導入する。その画面から送った文章は `assistant_outbound` として記録する。

## 保存する最小データ

- 注文スレッドID、LINEユーザー識別子、状態、要約
- お客様受信文、AI送信文、店長が明示記録した補足
- 受取／配達／来店の種別、候補日・時間、承認状態

住所・決済情報・本人確認書類は保存・抽出しない。メッセージ本文は注文処理に必要な期間だけ保持し、将来的に削除期限を設定する。

## 接続前の状態

顧客向けチャネルのシークレットとアクセストークン、業務カレンダーIDは未設定。接続前は、Webhook処理・お客様返信・カレンダー書込みを実行しない。
