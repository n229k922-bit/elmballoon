# LINE統括マネージャーによるHP一元管理仕様

## 1. 目的

店長は統括マネージャーLINEだけを操作し、HPの掲載内容を更新できるようにする。コード、GitHub、WordPress、Cloudflareの管理画面を直接操作しない。

対象はトップページ、お知らせ、商品、商品カテゴリー、固定ページ、FAQ、営業日、営業時間、連絡先、注文導線、画像、公開状態とする。

## 2. 絶対ルール

1. LINEで依頼しただけでは公開しない。
2. AIが変更内容と影響ページを復唱し、プレビューを作る。
3. 許可済み店長アカウントが「公開確定」と返信したときだけ公開する。
4. 削除は原則として非公開化とし、元に戻せる状態を保つ。
5. すべての変更を監査ログへ保存する。
6. お客様向けLINEからHPを変更できないよう、店長LINEと権限を分離する。
7. 電話番号、住所、営業時間など全ページ共通情報は一か所だけ更新し、全ページへ反映する。

## 3. LINEで管理する情報

| 対象 | LINEでできる操作 | 公開前確認 |
|---|---|---|
| 営業日 | 休業、休業解除、営業時間変更 | 必須 |
| お知らせ | 下書き、画像追加、公開、修正、非公開 | 必須 |
| 商品 | 写真から登録、名称・価格・用途・説明・カテゴリー設定 | 必須 |
| 商品カテゴリー | 作成、名称変更、並べ替え、非公開 | 必須 |
| トップページ | メイン画像、案内文、バナー、表示順の変更 | 必須 |
| 固定ページ | お店紹介、ゴム風船、アイブレックス等の文章・画像変更 | 必須 |
| FAQ | 質問と回答の追加、修正、並べ替え、非公開 | 必須 |
| 店舗情報 | 電話、住所、営業時間、定休日、SNS、LINE URL | 必須 |
| 注文導線 | ボタン文言、リンク先、受付停止・再開 | 必須 |

## 4. 共通会話フロー

1. 店長がLINEで変更を依頼する。
2. 統括マネージャーが対象と操作を判定する。
3. 情報不足があれば一度に必要事項を質問する。
4. 変更案を下書きとしてD1へ保存する。
5. プレビューURLと変更前後の要約をLINEへ返す。
6. 店長が「修正」と返信した場合は下書きだけを直す。
7. 店長が「公開確定」と返信した場合に公開データへ反映する。
8. 公開後、対象URLと完了内容をLINEへ返す。
9. 問題がある場合は「直前に戻す」で一つ前の版へ戻す。

確認トークンの有効期限は30分とし、期限後は再確認する。同じLINEイベントが再送されても二重公開しない。

## 5. 操作例

### 営業日

- `明日は休み`
- `休業 2026-10-15`
- `営業時間 2026-10-16 13:00-16:00`
- `休業解除 2026-10-15`

### お知らせ

- `お知らせを追加。タイトル「10月の店休日」、本文「…」`
- 画像を送ってから `この画像でお知らせを作成`
- `最新のお知らせを修正`
- `10月の店休日のお知らせを非公開`

### 商品

1. 商品写真をLINEへ送信する。
2. `新商品として登録` と送る。
3. AIが画像から候補を作り、名称、価格、用途、色、サイズ、カテゴリー、在庫状態を質問する。
4. 商品カードと商品詳細のプレビューを返す。
5. `公開確定` で公開する。

商品番号はシステムが採番し、同じ画像・同じLINEイベントによる重複登録を防ぐ。

### FAQ・固定ページ

- `FAQ追加：風船は何日もちますか？ 回答：…`
- `お店紹介の営業時間の文章を変更`
- `トップ画像をこの写真に変更` の後に画像を送る。

## 6. データ構造

D1を正本とし、HPは公開APIから表示する。GitHub内のJSONは障害時のフォールバックだけに使用する。

主要テーブル：

- `site_settings`: 電話、住所、営業時間、SNS、LINE URL
- `site_pages`: 固定ページ本文と公開状態
- `site_sections`: トップページ等のセクションと表示順
- `news_posts`: お知らせ
- `products`: 商品基本情報
- `product_images`: 商品画像
- `product_categories`: 商品カテゴリー
- `product_category_links`: 商品とカテゴリーの関連
- `faq_items`: FAQ
- `business_schedule`: 営業日・時間変更
- `content_drafts`: LINEから作成した未公開変更
- `content_versions`: 公開履歴と復元用データ
- `audit_log`: 操作者、時刻、変更前後、結果
- `processed_line_events`: LINEイベントの重複排除

画像はR2へ保存し、D1にはURL、代替テキスト、表示順、公開状態を保存する。

## 7. 公開API

- `GET /api/site-settings`
- `GET /api/business-schedule`
- `GET /api/pages/:slug`
- `GET /api/news`
- `GET /api/news/:slug`
- `GET /api/products`
- `GET /api/products/:id`
- `GET /api/categories`
- `GET /api/faq`
- `GET /api/preview/:token`（店長だけが知る短期URL）

公開APIは読み取り専用とする。書き込みは署名検証済みLINE Webhookからだけ行う。

## 8. 権限と安全対策

- `ADMIN_LINE_USER_IDS` に登録されたLINE userIdだけを管理者として扱う。
- LINE署名を必ず検証する。
- 「確定」ではなく、HP更新専用の `公開確定` を使用する。
- 電話番号・住所・注文受付状態の変更は、変更前後を強調して再確認する。
- 画像は種類、容量、縦横サイズを検査し、公開用へ変換する。
- HTMLを直接保存せず、許可した構造化データだけを保存する。
- 削除、公開、復元は監査ログへ記録する。
- シークレットやアクセストークンをGitHubへ保存しない。

## 9. 障害時

- API停止時は最後に正常取得したデータまたは同梱JSONを表示する。
- 最新情報を取得できない場合はHPへ注意文を表示する。
- LINEへの返信に、失敗した対象と再試行方法を明示する。
- 公開処理の途中で失敗した場合は公開版を変更せず、下書きを保持する。

## 10. 実装順序

1. 営業日カレンダー（実装済み）
2. 共通店舗情報API
3. お知らせのLINE下書き・プレビュー・公開
4. 商品画像登録と商品API
5. FAQ・固定ページ管理
6. トップページのセクション管理
7. バージョン復元、監査画面、総合テスト

各段階で、店長LINE以外から更新できないこと、確認前に公開されないこと、二重登録されないことを試験する。
